syntax = "proto3";
package protos;
option go_package = "./";

message Student {
    string id = 1;
    string name = 2;
    string email = 3;
}

service StudentService {
    rpc AddStudent (Student) returns (Student);
}